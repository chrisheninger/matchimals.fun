# OSX
.DS_Store

# Claude Code local state (session permissions, worktrees)
.claude/

# Expo / generated native project (CNG — regenerate with `bun run prebuild`)
/ios/
/build/
.expo/
/dist/
*.jsbundle

# Print-ready deck PDFs and previews (regenerate with `bun run print:cards`)
/print/

# node.js
node_modules/
npm-debug.log

# testing
/coverage

# Temporary files created by Metro to check the health of the file watcher
.metro-health-check*

.env

# App Store Connect API keys never belong in the repo
*.p8

# Localized App Store previews (regenerated by the marketing site's recorder;
# only the en-US pair at the top of store/previews is committed)
store/previews/*/
